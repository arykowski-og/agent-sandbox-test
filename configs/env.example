# Copy this file to .env and add your actual API key
OPENAI_API_KEY=your_openai_api_key_here

# Persistence Configuration
# Options: "memory", "sqlite", "postgres"
PERSISTENCE_TYPE=sqlite

# Database URL for PostgreSQL (optional, only needed if using postgres persistence)
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# SQLite database file (optional, defaults to chat_history.db)
# SQLITE_DB_PATH=chat_history.db 

OG_PLC_CLIENT_ID=
OG_PLC_SECRET=
OG_PLC_BASE_URL=